To setup the database go to `http://localhost/phpmyadmin/` and create a new database `volunteer_coordination`. 
Select the new database from the menu on the left. 
Click `SQL` from the menu of on the top and copy the contents of the file `db.txt` to the sql command line.

// User passwords are passwordx where x is user id.